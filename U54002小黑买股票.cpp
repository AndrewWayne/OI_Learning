#include <cstdio>
#include <iostream>
#include <set>
#include <algorithm>
#include <cmath>
using namespace std;
const int maxn = 1e3+10;
multiset<int> num_set;
int n, q, opt, l, r, val, k, num, sn, opt;
int main(){
    cin >> n >> q;
    for(int i = 1; i <= sn + 3; i++){
        bar_set[i].pre = i-1;
        bar_set[i].nxt = i+1;
    }

    for(int i = 1; i <= n; i++){
        cin >> val;

    }
    return 0;
}
