#include <cstdio>
#include <iostream>
#include <algorithm>
using namespace std;
const int maxn = 1e5 + 10;
int n, m, t, sum_row[maxn], sum_column[maxn];//row为横行感兴趣数，column为纵行
int main(){
    cin >> n >> m >> t;
    for(int i = 1; i <= t; i++)
        cin >> x1 >> y1, sum_row[x1]++, sum_column[y1]++;


    return 0;
}
